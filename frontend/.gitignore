# Node dependencies
/node_modules/

# Build output
/dist/
/build/
/public/build/

# VS Code settings
.vscode/
!.vscode/settings.json   # keep if you have shared workspace settings
!.vscode/extensions.json  # keep if you track recommended extensions
!.vscode/launch.json      # keep if you track debug configs
!.vscode/tasks.json       # keep if you track task configs

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
logs/
*.log

# Runtime data
pids/
*.pid
*.seed
*.pid.lock

# Directory for instrumented libs generated by jscoverage/Jest
coverage/

# Optional npm cache directory
.npm/

# Optional eslint cache
.eslintcache

# dotenv environment variables
.env
.env.*
!.env.example   # keep an example file

# Mac filesystem
.DS_Store

# Windows filesystem
Thumbs.db
ehthumbs.db
Desktop.ini

# SQL Server local database files (if any)
/*.mdf
/*.ldf

# Optional REPL history
.node_repl_history

# Yarn v2+
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/releases
!.yarn/plugins
!.yarn/sdks
!.yarn/versions

# Lock files (commit one, ignore others if using multiple managers)
# Uncomment the one you don’t use, or leave both if you switch managers
# package-lock.json
# yarn.lock
# pnpm-lock.yaml

# IDEs and editors
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln

# Temporary folders/files
tmp/
temp/
*.tmp
*.swp
*.swo
*.bak
*.backup

# Docker (if you ever dockerize this)
/docker-compose.override.yml
